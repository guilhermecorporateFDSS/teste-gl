// Run after build while the local preview server is running.
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const fs=require('node:fs');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage({reducedMotion:'reduce'});await page.goto('http://127.0.0.1:4173');await page.waitForSelector('h1');const app=await page.locator('#app').innerHTML();const schema=await page.locator('script[type="application/ld+json"]').textContent();let html=fs.readFileSync('dist/index.html','utf8');html=html.replace('<div id="app"></div>',()=>`<div id="app">${app}</div>`).replace('</head>',()=>`<script type="application/ld+json">${schema}</script></head>`);fs.writeFileSync('dist/index.html',html);await browser.close();console.log('Conteúdo e schema pré-renderizados para SEO.');})().catch(e=>{console.error(e);process.exit(1)});
